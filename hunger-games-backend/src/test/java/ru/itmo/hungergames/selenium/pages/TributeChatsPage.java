package ru.itmo.hungergames.selenium.pages;

import org.openqa.selenium.WebDriver;

public class TributeChatsPage extends ChatsPage {
    public TributeChatsPage(WebDriver driver) {
        super(driver);
    }
}
